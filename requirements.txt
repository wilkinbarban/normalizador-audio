# Python runtime policy:
# The installers detect the Python version already installed on the system and
# treat that major.minor version as the minimum runtime for the local setup.
# Example: Python 3.14 installed => local dependency floor is Python >=3.14.
PyQt6>=6.0.0
qrcode[pil]>=8.0.0
requests>=2.31.0
packaging>=24.0
pytest>=9.0.0
pytest-cov>=7.1.0
bandit>=1.7.0
pylint>=3.0.0
